describe("something to be truthy", () => {
  it("true to be true", () => {
    expect(true).toBe(true);
  });
});

describe("something to be false", () => {
  it("false to be false", () => {
    expect(false).toBe(false);
  });
});
